pipeline {
    agent any

    stages {
        stages {
        stage('compile') {
            steps {
            	withMaven(maven:'Maven')
                sh 'mvn clean compile' 
            }
        }
 	}
        stages {
        stage('Test') {
            steps {
                withMaven(maven:'Maven')
                sh 'mvn clean test' 
            }
        }
    }
  }
}